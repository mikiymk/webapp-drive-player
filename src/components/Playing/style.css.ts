import { style } from "@vanilla-extract/css";

export const stylePlaying = style({
  height: "100%",
});

export const styleImage = style({
  height: "auto",
  maxHeight: "100%",
  width: "100%",
  objectFit: "contain",
});
